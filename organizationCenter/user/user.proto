syntax = "proto3";

package organizationCenter;

option go_package=".";

service userService {
  rpc Greet(Request) returns (Response) {}
}

message Request {
  string name = 1;
}

message Response {
  string msg = 1;
}
